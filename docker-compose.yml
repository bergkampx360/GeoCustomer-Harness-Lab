services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-geocustomer}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-geocustomer}
      POSTGRES_DB: ${POSTGRES_DB:-geocustomer}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-geocustomer} -d ${POSTGRES_DB:-geocustomer}"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres-data:
