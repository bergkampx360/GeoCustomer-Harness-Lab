# Testing Requirements

- Test framework: Vitest.
- The Haversine distance calculation must have unit tests covering:
  - a known distance — Budapest–Vienna, approximately 214 km;
  - the zero-distance case — Budapest to itself;
  - null-coordinate handling.
- Additional focused tests may be added beyond these, but only when they provide clear value — they are not required.
