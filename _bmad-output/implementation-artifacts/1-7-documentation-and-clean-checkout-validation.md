---
baseline_commit: 7b592cf
---

# Story 1.7: Documentation and Clean-Checkout Validation

Status: done

## Story

As a reviewer verifying the assignment,
I want a README that documents the full run sequence and a genuine fresh-clone dry run,
so that I can confirm the whole service works from nothing but the repository and the README.

## Acceptance Criteria

(Verbatim from `_bmad-output/planning-artifacts/epics.md`, Story 1.7 — do not reinterpret or broaden.)

1. Given a fresh clone (or an equivalent clean-state simulation) with only the README as guidance, when every documented step is followed in order (Postgres start, migration, seed, server start, tests), then Postgres starts, the migration applies, the seed runs idempotently, the server starts, both endpoints respond correctly, and `pnpm vitest run` passes (NFR6).
2. Given the README, when inspected, then it does not instruct committing the real `.env` file, and references `.env.example` instead.
3. Given this branch's new commits, when inspected, then no secrets or real `.env` file are present in git history.

## Tasks / Subtasks

- [x] Task 1: Replace `README.md` with real project documentation (AC: 1, 2)
  - [x] Remove the current Nx-boilerplate content (the "Your new, shiny Nx workspace" default generated by `create-nx-workspace` — confirmed still in place at repo root, never edited since Story 1.1) and replace it with documentation of this specific service.
  - [x] Document the exact run sequence, each step giving the literal command to run:
    1. `pnpm install`
    2. Copy `.env.example` to `.env` (do not commit the real `.env`)
    3. `docker compose up -d` — starts the `postgres:16-alpine` container (per `docker-compose.yml`, AD-9)
    4. `pnpm exec prisma migrate deploy --schema apps/api/prisma/schema.prisma` — applies the existing, already-committed `20260716160812_init` migration. **Use `migrate deploy`, never `migrate dev`, in the clean-checkout workflow:** `migrate deploy` only applies committed migrations and never generates a new one or prompts interactively, which is exactly what a non-interactive clean-checkout dry run requires; `migrate dev` can create/modify migration files and is a dev-authoring command, not a deployment command, and its use is explicitly excluded from this story (see Dependencies and Exclusions below).
    5. `npx nx run api:seed` (or `pnpm exec nx run api:seed`) — runs the idempotent seed (Story 1.3); note it is safe to re-run
    6. `npx nx run api:serve` (or equivalent) — starts the Fastify server on port 3000 (`apps/api/src/server.ts`)
    7. `curl http://localhost:3000/customers/count` and `curl http://localhost:3000/customers/by-distance` — manual verification of both endpoints
    8. `pnpm exec vitest run` — runs the full test suite (domain unit tests + `customers.spec.ts` route tests, per `vitest.config.ts`'s `apps/**/src/**/*.spec.ts` include pattern)
  - [x] State explicitly that `.env.example` is the template for `.env`, and that the real `.env` must never be committed (reinforces AC2 and the existing `.gitignore` `.env` / `.env.*` / `!.env.example` rules).
  - [x] Do not invent commands not already established by prior stories' `project.json` targets (`build`, `serve`, `seed`) or root `package.json` — verify each documented command actually exists before writing it (see Dev Notes below for the authoritative target list).
  - [x] Keep it concise: this is a small single-service repo, not a large product — avoid restating the whole architecture spine or SPEC; link to `_bmad-output/planning-artifacts/` for anyone wanting the full design record instead of duplicating it.

- [x] Task 2: Perform one literal clean-checkout dry run and record the evidence (AC: 1)
  - [x] Simulated a genuinely clean checkout: `git clone --branch harness/bmad --single-branch` from the local repo into a disposable, uniquely-named `/tmp` directory (own `.git`, no shared worktree, no reused `node_modules`/`dist`/Nx cache/generated Prisma Client), then followed only the README's documented sequence.
  - [x] Executed every documented step in order with a fully isolated, uniquely-named Docker Compose project. The accepted run (see "CORRECTED clean-checkout validation" in Debug Log References, the acceptance evidence for AC1) used host port `5440` and Compose project `geocustomer-revalidate-20260719-001118-3404`; an earlier, disclaimed preliminary run (superseded — not acceptance evidence) had used port `5434` — see Debug Log References for the full transcript of the accepted run: `pnpm install --frozen-lockfile` success, `docker compose ... up -d --wait` healthy, `prisma migrate deploy` applied (never `migrate dev`, no migration file created/modified), seed run twice with a direct `psql` row-count check proving idempotency (15 both times), server start with a readiness loop, both endpoints verified with JSON-content assertions (not just HTTP status), `pnpm exec vitest run` full pass, `tsc --noEmit` pass, `nx run api:build` pass.
  - [x] Recorded the full evidence transcript below in Debug Log References, including the validated branch/commit SHA, per this project's evidence-recording convention.
  - [x] Cleanup was performed and confirmed for the accepted run: explicit, sequential teardown (no `trap`, per the "CORRECTED" run's own Debug Log entry) — Node 24 helper container stopped/removed, disposable Docker Compose project torn down with `down -v --remove-orphans` (its own throwaway volume only), both disposable temp clones removed. The earlier, disclaimed preliminary run had used a `trap`-based handler instead; that detail applies only to the superseded run, not the accepted one. The real project's `bmad` Compose project, `bmad_postgres-data` volume, and the unrelated `superpowers-db-1`/`plantbase-pg` containers were never targeted or touched by either run.

- [x] Task 3: Verify no secrets or the real `.env` file are anywhere in git history (AC: 3)
  - [x] Run `git log --diff-filter=A -- .env` scoped to `harness/bmad` to confirm `.env` was never added to any commit in this branch's history.
  - [x] Run a `git log -p` scan across `harness/bmad`'s own commits for credential-shaped strings (real passwords, tokens, connection strings with non-placeholder values) — found none; the only connection strings present anywhere in this branch's history are the `changeme`/`changeme` and `placeholder`/`placeholder` template values, consistent with `.env.example` and `.mcp.json`'s `${DATABASE_URL}` passthrough (already verified clean in Story 1.6).
  - [x] Confirm `README.md` itself contains no instruction to commit `.env` and explicitly references `.env.example` as the setup source (AC2, direct text check).

### Review Findings

- [x] [Review][Patch] Task 2 checklist bullets cited the superseded run's port (`5434`) and cleanup method (`trap`-based) instead of the accepted run's actual details (port `5440`, explicit sequential cleanup, no `trap`) [1-7-documentation-and-clean-checkout-validation.md: Task 2] — fixed in this review.
- [x] [Review][Defer] README's "Start PostgreSQL" and endpoint-verification steps give no troubleshooting guidance for port collisions or a not-yet-ready container/server, despite the story's own validation transcript hitting exactly this class of issue twice — deferred, not required by AC1/Task 1, pre-existing scope boundary (story explicitly scopes README to conciseness).
- [x] [Review][Defer] README's `.env` setup step doesn't repeat `.env.example`'s own warning that `DATABASE_URL` is not auto-derived from the other variables — deferred; low risk since the documented sequence never instructs changing `POSTGRES_PORT`.
- [x] [Review][Defer] The accepted validation's temporary commit (`560b60085efc234722390362815fcb15235733d1`) was deleted along with its disposable clone, so it cannot be independently re-verified by a future reviewer — deferred; already adjudicated as an approved, settled correction for this closure review, and the underlying claim (5ce0b36 lacked the new README) was independently re-confirmed via `git show 5ce0b36:README.md` during this review.
- [x] [Review][Defer] `sprint-status.yaml`'s Story 1.7 `verification_gate` text still describes only the plain README-driven flow, not the extra harness-only scaffolding (Docker Node 24 container, corepack pnpm pin, ambient-env-var correction) the accepted run needed — deferred, documentation-polish only, doesn't loosen or misstate the gate's substance.

### Previous Story Intelligence (from Story 1.6)

- **This is the final integration/validation story — no new application functionality.** Per epics.md: "No new functionality. Documentation and verification only." Do not touch `apps/api/src/**`, `docker-compose.yml`, `.mcp.json`, the Prisma schema, or the seed logic — those are all already correct and complete from Stories 1.1–1.6.
- **Docker Compose discipline established across all prior stories:** `docker compose up -d`, verify healthy/seeded, do the work, then `docker compose down` afterward — volume preserved, no unrelated containers touched. Apply the same discipline to Task 2's dry run.
- **Nx/pnpm invocation note:** this shell environment sets `CLAUDECODE`/`OPENCODE`, switching Nx's CLI into an agent-oriented NDJSON output mode. Prefix plain `nx`/`pnpm nx` commands with `env -u CLAUDECODE -u OPENCODE` for standard, human-readable output when capturing evidence for the README/dry-run transcript.
- **Seeded row count is 15** — stable since Story 1.3, re-verified in Stories 1.4, 1.5, and 1.6 (including via live MCP query). Task 2's dry-run seed step must reproduce this exact count, twice.
- **Commit pattern established across Stories 1.2–1.6:** two commits per story, "Prepare Story X.Y ..." then "Implement Story X.Y ...". This story should follow the same pattern when it reaches implementation (not performed by this story-creation step).
- **Evidence-recording convention:** Story 1.6 established that a verification step is not complete until its evidence is recorded in the story file itself (transcript/log excerpt in Dev Agent Record), not merely performed and discarded. Apply the same standard to Task 2's dry run.

### Git Intelligence Summary

Recent commit pattern (each story = two commits, "Prepare" then "Implement"):
```
7b592cf Implement Story 1.6 PostgreSQL MCP integration
bd66e80 Prepare Story 1.6 PostgreSQL MCP integration
8561b49 Implement Story 1.5 distance endpoint
baa135b Prepare Story 1.5 distance endpoint
7521979 Implement Story 1.4 customer count endpoint
```
`README.md` has not been touched in any commit since the initial Nx scaffold — it is untouched, default-generated content. This story is the first to modify it. Expected diff: `README.md` rewritten in place, plus this story file's own updates (dry-run evidence recorded in Dev Agent Record) — no `apps/api/**` changes, no `docker-compose.yml`/`.env.example`/`.mcp.json` changes.

## Dev Notes

- **Architecture governance:** the Structural Seed in `ARCHITECTURE-SPINE.md` places `README.md` at the repo root, sibling to `docker-compose.yml`, `.env.example`, and `.mcp.json`. No `AD` specifically governs README content beyond what `repo-and-docs-quality.md` (SPEC companion) requires: document starting Postgres, running the migration, running the seed, starting the server, running the tests. [Source: architecture/architecture-GeoCustomer-Harness-Lab-2026-07-13/ARCHITECTURE-SPINE.md, Structural Seed]
- **`repo-and-docs-quality.md` is the authoritative source for this story's documentation requirements** — read it in full before writing the README; it also states the development-process rules (small focused commits, verify before commit, no hidden/bypassed failures, no weakened tests, no committed secrets, no committed real `.env`) that Task 3 verifies retroactively for this branch. [Source: spec-geocustomer-backend/repo-and-docs-quality.md]
- **Current `README.md` is 100% unmodified Nx boilerplate** (`create-nx-workspace` default) — confirmed by direct read during story creation. It contains no project-specific information and must be replaced, not appended to.
- **Authoritative list of real, existing commands to document** (verified directly against `apps/api/project.json`, root `package.json`, and `docker-compose.yml` during story creation — do not invent alternatives):
  - `pnpm install` (root `package.json` has a `postinstall: "prisma generate"` hook — runs automatically)
  - `docker compose up -d` / `docker compose down` (root `docker-compose.yml`, one `postgres` service)
  - Prisma migration: no root-level script wraps this; the required command is `pnpm exec prisma migrate deploy --schema apps/api/prisma/schema.prisma` — applies the one existing, already-committed migration (`20260716160812_init`) without prompting. `migrate dev` is explicitly not to be used or documented for the clean-checkout workflow (it is a migration-authoring command that can create/modify migration files; this story only validates applying what is already committed — see Dependencies and Exclusions).
  - Seed: `nx run api:seed` (defined in `apps/api/project.json` as `tsx apps/api/src/seed.ts`)
  - Serve: `nx run api:serve` (defined in `apps/api/project.json`, `@nx/js:node` executor, depends on `build`; server listens on port 3000 per `apps/api/src/server.ts` — hardcoded, no `PORT` env override exists, a known deferred item from Story 1.2's review, not in scope to fix here)
  - Tests: `pnpm vitest run` (root `vitest.config.ts` includes `apps/**/src/**/*.spec.ts`; five spec files currently exist: `distance.spec.ts`, `sort.spec.ts`, `normalize.spec.ts`, `settlement-coordinates.spec.ts`, `customers.spec.ts`)
  - There is no root `package.json` script alias for any of the above except `postinstall` — commands must be documented as direct `pnpm`/`npx nx`/`docker compose` invocations, not `pnpm run <script>`.
- **`.env.example` already exists and is complete** (`POSTGRES_USER`, `POSTGRES_PASSWORD`, `POSTGRES_DB`, `POSTGRES_PORT`, assembled `DATABASE_URL`, all placeholder `changeme`/`geocustomer` values) — the README must tell the reader to copy it to `.env`, not describe inventing new variables.
- **Both endpoints and their exact shapes** (for the README's or dry-run's manual verification step): `GET /customers/count` → `{"count": 15}`; `GET /customers/by-distance` → array of customers ascending by raw Haversine distance from Budapest, each with `distanceKm` (number, one decimal, or `null` if unresolved), Budapest customers first at `0`, unresolved-location customers last. [Source: epics.md, Story 1.4 / Story 1.5 acceptance criteria]
- **Do not touch:** `apps/api/src/**`, `docker-compose.yml`, `.env.example`, `.mcp.json`, `apps/api/prisma/**`, root `package.json`, `vitest.config.ts`. This story's only expected file change is `README.md`; the only other change is this story file's own Dev Agent Record.
- **Scope discipline:** no CI workflow, no linting setup, no new npm scripts — `deferred-work.md` (Story 1.1 review) already explicitly deferred CI/lint infrastructure as out of approved scope; this story does not reopen that decision.

### Project Structure Notes

Builds directly on Stories 1.1–1.6's foundation; modifies exactly one existing repo-root file. Expected structure after this story (only what this story changes):

```text
{repo-root}/
  README.md                   # REWRITTEN — replaces Nx boilerplate with real run-sequence docs
  .env.example                # UNCHANGED — already placeholder-only, already has DATABASE_URL
  docker-compose.yml          # UNCHANGED
  .mcp.json                   # UNCHANGED
  apps/
    api/                      # UNCHANGED — no application code in this story
```

No `apps/api/src` changes, no new Nx target, no new npm script.

### References

- [Source: planning-artifacts/epics.md, Story 1.7] — approved scope, acceptance criteria, objective, dependencies, exclusions, validation commands
- [Source: spec-geocustomer-backend/repo-and-docs-quality.md] — README content requirements (5 run-sequence items) and development-process rules (no committed secrets, no committed real `.env`, `.env.example` convention)
- [Source: architecture/architecture-GeoCustomer-Harness-Lab-2026-07-13/ARCHITECTURE-SPINE.md, Structural Seed] — `README.md` repo-root placement
- [Source: apps/api/project.json] — authoritative Nx target list (`build`, `serve`, `seed`; no `test` target — tests run via root `pnpm vitest run`)
- [Source: vitest.config.ts] — test include pattern (`apps/**/src/**/*.spec.ts`), confirming `pnpm vitest run` is the correct root-level test command
- [Source: apps/api/src/server.ts] — confirms port 3000, no `PORT` env override, `SIGINT`/`SIGTERM` graceful shutdown behavior worth a one-line README mention
- [Source: .env.example, docker-compose.yml, .gitignore] — current state read directly during story creation; confirmed `.env` is git-ignored, `.env.example` is the committed placeholder template
- [Source: _bmad-output/implementation-artifacts/1-6-postgresql-mcp-integration-and-verification.md] — established Docker Compose discipline, commit pattern, Nx/pnpm invocation note, evidence-recording convention
- [Source: _bmad-output/implementation-artifacts/deferred-work.md] — confirms CI/lint infrastructure is out of scope (Story 1.1 review), and that `server.ts`'s hardcoded port is a known, accepted, unfixed deferral (Story 1.2 review) — not to be silently "fixed" as scope creep in this story

## Dependencies and Exclusions

**Dependencies:** Stories 1.1–1.6 — this is the final integration and validation pass; no new functionality.

**Explicitly excluded from this story** (reserved for later stories or permanently out of scope):

- No new functionality of any kind — documentation and verification only.
- No CI workflow, no linting setup, no new npm/Nx scripts.
- No fix to the `server.ts` hardcoded port or any other previously-deferred item not required by this story's own acceptance criteria.
- No application code changes under `apps/api/src`, no changes to `docker-compose.yml`, `.env.example`, `.mcp.json`, or the Prisma schema/migrations.
- **`prisma migrate deploy` only — never `prisma migrate dev` — in the clean-checkout workflow, in the README, or anywhere in this story's evidence.** `migrate dev` must not be run, documented, or referenced as an alternative during this story: it can create or modify migration files, which is authoring behavior out of scope for a validation-only story. No migration file may be created or modified as part of this story; the clean-checkout dry run applies only the already-committed `20260716160812_init` migration via `migrate deploy`.

## Validation Commands

Run all of the following and confirm the stated result before moving this story to review:

- A literal clean-checkout (or equivalent clean-state simulation) dry run, following only `README.md`, completes end to end: `pnpm install` succeeds; `docker compose up -d` starts a healthy `postgres:16-alpine` container; the migration applies via `prisma migrate deploy` (never `migrate dev`, and no migration file is created or modified); the seed runs and, run a second time, still shows exactly 15 rows (idempotent); the server starts; `curl` against both `GET /customers/count` and `GET /customers/by-distance` returns correct responses; `pnpm vitest run` passes in full.
- Manual inspection of `README.md` confirms it never instructs committing the real `.env` file and explicitly references `.env.example`.
- `git log --all --diff-filter=A -- .env` (or equivalent) plus a credential-string scan across this branch's commit history confirms no secret or real `.env` file was ever committed.

## Definition of Done

- [x] All 3 acceptance criteria verified against the running implementation
- [x] All Validation Commands above pass with the stated result
- [x] No excluded item (new functionality, CI/lint setup, unrelated deferred-item fixes, application code changes) was added
- [ ] Lands as its own small, focused commit — pending: not yet staged/committed by design (finalization step does not stage, commit, or push)
- [x] Passed code review before being marked done

## Evidence Expected Before Review

- The rewritten `README.md` content
- A recorded transcript or log excerpt of the clean-checkout dry run: install, Postgres start, migration, seed (twice, showing idempotency), server start, both endpoint `curl` responses, full `pnpm vitest run` output
- Confirmation (e.g. `git log`/`git grep` output or explicit statement) that no secret or real `.env` file exists anywhere in this branch's git history

## Dev Agent Record

### Agent Model Used

Claude Sonnet 5 (claude-sonnet-5)

### Debug Log References

**Task 3 — git history secret scan (scoped to `harness/bmad`, not `--all`):**

Initial `--all`-scoped scan surfaced a `POSTGRES_PASSWORD=geocustomer` / `postgresql://geocustomer:geocustomer@localhost:5432/geocustomer` value in old commits — investigated and confirmed those commits (`d6648a7`, `787fb3b`, `0b4d1b3`, `f7fdbf2`, `aacf0f6`) belong exclusively to `origin/harness/superpowers`, a sibling harness branch, and are not ancestors of `harness/bmad` (`git merge-base --is-ancestor d6648a7 harness/bmad` → not an ancestor). AC3 scopes the check to "this branch's new commits," so the scan was re-run scoped to `harness/bmad` only:

```
$ git log harness/bmad --diff-filter=A -- .env
(no output — .env was never added in any harness/bmad commit)

$ git log harness/bmad -p | grep -oE 'postgresql://[^"'"'"'[:space:]]+' | sort -u
postgresql://changeme:changeme@localhost:5432/geocustomer
postgresql://placeholder:placeholder@localhost:5432/placeholder
postgresql://user:pass@host/db`   (doc prose, not a real value)

$ git log harness/bmad -p | grep -iE '^\+.*(PASSWORD|TOKEN|SECRET|API_KEY)\s*=' | grep -viE 'changeme|placeholder|\$\{|example'
+TEMPLATE_TOKEN = re.compile(r"\{[a-z_][a-z0-9_ /.-]*\}")   (unrelated Python regex identifier, not a credential)
```

Result: clean. `.env` was never committed on `harness/bmad`; every connection string ever present in this branch's history uses the `changeme`/`changeme` or `placeholder`/`placeholder` template values (already the current `.env.example` content); the one non-placeholder match is an unrelated source-code identifier, not a credential.

**Task 2 — clean-checkout validation transcript, PRELIMINARY RUN, SUPERSEDED — do not use as acceptance evidence (all commands run inside an isolated `/tmp` clone with a uniquely-named Docker Compose project, per an explicitly approved plan):**

> **Superseded finding (code review):** this run's own transcript claims the validated commit was `5ce0b36`, but `5ce0b36` (this story's own "Prepare Story 1.7 ..." commit) never contained the rewritten README — `git show 5ce0b36:README.md` is still the unmodified Nx `create-nx-workspace` boilerplate. The README rewrite existed only as an *uncommitted* working-tree change at the time of this run, so this transcript cannot prove the new README's instructions were the ones actually followed, and the commit/evidence chain is broken. This entire preliminary run is retained below for history only; the corrected, evidence-valid re-run is recorded further down under "Task 2 — CORRECTED clean-checkout validation (Node 24, temporary validation commit)." Additionally this preliminary run used the ambient host Node (`v26.4.0`), not the repository-declared `>=24 <25` range — a second reason it cannot stand as final acceptance evidence.

- Validated branch: `harness/bmad`. Validated commit: `git -C "$TMP_DIR" rev-parse HEAD` → `5ce0b36f791bfc1df45893c08916bbfe3e20fdc0` (this story's own "Prepare Story 1.7 ..." commit — the latest commit on the branch at the time of this dry run). `git -C "$TMP_DIR" status --short` → empty (clean clone, no uncommitted content of any kind carried over).
- Temp dir: `/tmp/geocustomer-clean-checkout-20260718-185218-20065`. Compose project: `geocustomer-clean-checkout-20260718-185218-20065`. Both destroyed by the `trap`-based cleanup at the end of the run.
- `.env`: created via `cp .env.example .env` only (the real, git-ignored `.env` in the working repo was never read or copied), then a non-secret `POSTGRES_PORT`/`DATABASE_URL` port edit (`5432` → `5434`) applied only to this disposable copy, to avoid colliding with two unrelated local Docker Postgres containers already bound to `5432`/`5433` on this machine.
- `pnpm install --frozen-lockfile` → succeeded (`Packages: +724`, lockfile unchanged, `postinstall` ran `prisma generate` successfully). One pre-existing, unrelated `engines` warning (`wanted node >=24 <25`, local Node is `v26.4.0`) — a machine-environment mismatch predating this story, not a regression introduced here.
- `docker compose -p <project> up -d --wait` → succeeded directly (no fallback readiness loop needed): container created, started, and reported **Healthy** by Compose's own `--wait`, honoring the existing `pg_isready`-based healthcheck in `docker-compose.yml`. `docker compose ... ps` confirmed `Up ... (healthy)`, port mapping `0.0.0.0:5434->5432/tcp`.
- `pnpm exec prisma migrate deploy --schema apps/api/prisma/schema.prisma` → "1 migration found in prisma/migrations", applied `20260716160812_init`, "All migrations have been successfully applied." No `migrate dev` was run at any point; no migration file was created, modified, or prompted for.
- Seed run #1: `[seed] processed 15 customers`; direct `psql -tAc 'SELECT COUNT(*) FROM customers;'` inside the disposable container → `15`.
- Seed run #2: `[seed] processed 15 customers`; same `psql` count query → `15`. **Idempotency confirmed**: identical row count (15) after both runs, no duplication.
- API server started in the background (`pnpm exec nx run api:serve`, log redirected to a file, PID captured); a readiness loop polled `GET /customers/count` until it returned successfully (also checking the process hadn't died) — became ready well within the 60 s budget.
- `GET /customers/count` → `{"count":15}` — exact match to the expected shape/value.
- `GET /customers/by-distance` → 15 items; JSON-content assertions (not just HTTP status) confirmed: 15 unique customer `id`s (no duplication reaching the API layer either), `countryCode` absent from every item, strictly ascending `distanceKm` with no `null`s present in this dataset (all 15 seed customers resolve to real coordinates, consistent with Story 1.3/1.5's established behavior), first item is the Budapest customer (Anna Kovács) at `distanceKm: 0`, second is Vienna at `distanceKm: 214` (matches the ≈214 km Budapest–Vienna reference distance from `distance.spec.ts`), remaining 13 customers in increasing order out to Lisbon at `distanceKm: 2469.4`.
- API stopped cleanly (`kill` + `wait` on the captured PID).
- `pnpm exec vitest run` → **5 test files, 19 tests, all passed** (`distance.spec.ts`, `sort.spec.ts`, `normalize.spec.ts`, `settlement-coordinates.spec.ts`, `customers.spec.ts`).
- `pnpm exec tsc --noEmit -p apps/api/tsconfig.app.json` → succeeded, no output (no type errors).
- `pnpm exec nx run api:build` → succeeded ("Successfully ran target build for project api"). This build was implicitly run once already as `serve`'s `dependsOn: ["build"]` earlier in this same disposable clone (Nx cached that intra-run result and reused it here, per Nx's per-workspace-root cache directory logic — the temp clone's own `.nx/cache`, confirmed to not be a shared/worktree cache since the temp clone is an independent `git clone`, not a linked worktree of the real repo); this is expected same-run Nx behavior, not a reuse of build output from the original checkout.
- Cleanup (`trap` on `EXIT`): API process already stopped; `docker compose -p <project> down -v --remove-orphans` removed exactly that project's container, network, and volume; `rm -rf` removed the temp directory. Confirmed by the compose output itself (`Container ... Removed`, `Volume ... Removed`, `Network ... Removed`) — no reference to `bmad`, `bmad_postgres-data`, `superpowers-db-1`, or `plantbase-pg` anywhere in the teardown.
- No commit, no push, and no real secret value was used, displayed, or logged at any point (all connection/credential values throughout were the `.env.example` placeholders, with only a non-secret port number changed).

**Task 2 — CORRECTED clean-checkout validation (Node 24, temporary validation commit) — this is the accepted evidence for AC1:**

This re-run fixes the broken commit/README evidence chain above and switches to the repository-declared Node runtime. It validates the *exact current Story 1.7 candidate* (the working-tree diff of `README.md`, this story file, and `sprint-status.yaml` on top of `5ce0b36`), not a commit that predates the README rewrite.

*Harness-only setup (not part of the README's user workflow):*
- First disposable clone: `git clone` of the local `harness/bmad` branch into a fresh `/tmp` directory (base commit `5ce0b36`, still boilerplate README, confirmed). The exact current candidate diff for the 3 changed files (`README.md`, this story file, `sprint-status.yaml`) was copied in from the real working tree — no other files.
- A repo-local-only git identity was configured inside this disposable clone (`git config user.name "Story 1.7 Validation"` / `user.email "story-1.7-validation@local.invalid"` — local to that clone's `.git/config`, global git config never touched).
- A **temporary, local-only validation commit** was created: SHA `560b60085efc234722390362815fcb15235733d1`. Verified immediately: `git show 560b6008...:README.md` → starts with `# GeoCustomer Harness Lab` (the new project-specific README), and a `grep -c "Nx workspace"` check on that same blob returned `0` — confirms this commit, unlike `5ce0b36`, actually contains the rewritten README.
- A **second clone** was made from the first disposable clone, giving a genuinely clean checkout of the temporary commit (not a dirty working copy). Verified before creating `.env`: `HEAD` == `560b6008...` (match), `git status --short` empty (clean), `git show HEAD:README.md` contains `GeoCustomer Harness Lab` (new README confirmed), and no `node_modules`, `dist`, `.nx`, `apps/api/node_modules`, `apps/api/generated`, or `.env` present anywhere in the checkout.
- Node 24 provided via Docker (no version manager available on the host, no global install performed): pulled `node:24-slim` — `docker exec ... node --version` → `v24.18.0`; image identity: `ImageID sha256:6f7b03f7c2c8e2e784dcf9295400527b9b1270fd37b7e9a7285cf83b6951452d`, `RepoDigest node@sha256:6f7b03f7c2c8e2e784dcf9295400527b9b1270fd37b7e9a7285cf83b6951452d`. A disposable helper container ran with the second clone bind-mounted in, joined to the disposable Compose project's network so it could reach Postgres by its internal Docker hostname `postgres`, with its own API port published to the host.
- pnpm version: the candidate's `package.json` has **no `packageManager` field and no `engines.pnpm`** — there is no repository-declared pnpm version. Per explicit instruction, pnpm **11.9.0** was selected as a validation-harness-only choice (matching the preliminary run above; not a claim that the repository pins this version) and activated via `corepack enable && corepack prepare pnpm@11.9.0 --activate` inside the container. `pnpm --version` → `11.9.0`. Adding a `packageManager` field was intentionally not made part of this story's scope; this is recorded as a non-blocking reproducibility note only.
- Disposable ports selected after verifying no existing listener on them: Postgres host port `5440`, API host port `13300`. Compose project name: `geocustomer-revalidate-20260719-001118-3404`. **Correction made mid-run:** the shell session had ambient `POSTGRES_USER`/`POSTGRES_PASSWORD`/`POSTGRES_DB`/`POSTGRES_PORT`/`DATABASE_URL` environment variables already set (Compose gives shell env vars priority over `.env`), which silently overrode the intended port on the first `up -d` attempt (bound `5434` instead of `5440`). Detected via `docker compose ... ps` showing the wrong port, torn down immediately, and restarted with those 5 variables explicitly unset for the Compose invocation (`env -u POSTGRES_USER -u POSTGRES_PASSWORD -u POSTGRES_DB -u POSTGRES_PORT -u DATABASE_URL docker compose ...`), after which the correct port (`5440`) was bound. The disposable `.env` file itself was built only from the second clone's own `.env.example`, with only the `POSTGRES_PORT` value and the `DATABASE_URL` host segment (`localhost` → `postgres`) changed — both recorded here only as this redacted structural description, full `DATABASE_URL` value never printed.

*README user workflow, executed in order inside the Node 24 container / against the isolated Postgres:*
- `pnpm install --frozen-lockfile` → succeeded; `postinstall` ran `prisma generate` → "Generated Prisma Client (7.8.0)". Non-fatal `node:24-slim`-specific OpenSSL-detection warning ("Defaulting to openssl-1.1.x") appeared but did not block install, migration, or serving.
- `docker compose -p geocustomer-revalidate-20260719-001118-3404 up -d --wait` (after the port correction above) → `Container ... Healthy`; `ps` confirmed `0.0.0.0:5440->5432/tcp`.
- `pnpm exec prisma migrate deploy --schema apps/api/prisma/schema.prisma` → "1 migration found", applied `20260716160812_init`, "All migrations have been successfully applied." Datasource resolved to `postgres:5432` (internal Docker hostname, confirming cross-container network isolation worked). No `migrate dev` used.
- Seed run #1: `[seed] processed 15 customers`. Seed run #2: `[seed] processed 15 customers`. Direct `psql -U changeme -d geocustomer -tAc 'SELECT COUNT(*) FROM customers;'` → `15`. Idempotency confirmed.
- API server started in the background inside the container (log + PID captured); a readiness loop polled `http://localhost:13300/customers/count` from the host — ready after 4s.
- `GET /customers/count` → `{"count":15}`.
- `GET /customers/by-distance` → 15 items; content-level assertions: 15 unique `id`s, `countryCode` absent from every item, strictly ascending `distanceKm` (no `null`s), first = Anna Kovács (Budapest) at `distanceKm: 0`, last = Lisbon at `distanceKm: 2469.4`.
- API stopped cleanly (`kill` on the captured PID; log confirmed no further activity after the stop).
- `pnpm exec vitest run` → **5 test files, 19 tests, all passed** (`normalize.spec.ts`, `settlement-coordinates.spec.ts`, `distance.spec.ts`, `sort.spec.ts`, `customers.spec.ts`), duration 499ms.

*Harness-only additional checks (not part of the README):*
- `pnpm exec tsc --noEmit -p apps/api/tsconfig.app.json` → exit code `0`, no type-error output.
- `npx nx run api:build` → "Successfully ran target build for project api" (served from this run's own local Nx cache, since `serve`'s `dependsOn: ["build"]` had already run it once in this same disposable checkout).

*Cleanup (harness-only, all explicit — no `trap`, run sequentially at the end):*
- Node 24 helper container stopped and removed.
- `docker compose -p geocustomer-revalidate-20260719-001118-3404 down -v --remove-orphans` (with the same 5 ambient vars unset) → `Container ... Removed`, `Volume ... Removed`, `Network ... Removed`.
- Confirmed via `docker ps -a` / `docker network ls` / `docker volume ls` filtered on the project name → all three empty.
- Entire disposable `/tmp` tree (both clones) removed via `rm -rf`; confirmed absent afterward.
- No real `harness/bmad` Docker Compose project, container, network, or volume was ever referenced or touched by any command in this corrected run.
- No commit, staging, or push occurred in the real `harness/bmad` working tree at any point; the temporary validation commit (`560b6008...`) existed only inside the now-deleted disposable clone and no longer exists anywhere.

### Completion Notes List

- Task 1: `README.md` fully rewritten, replacing the untouched `create-nx-workspace` boilerplate. Documents: prerequisites, clone/entry, `.env` setup (with the "never commit `.env`" warning stated explicitly), `pnpm install`, `docker compose up -d`, `prisma migrate deploy` (with an explicit callout that `migrate dev` must never be used here), the seed command, the serve command, both endpoint `curl` checks with expected responses, `pnpm exec vitest run`, an optional PostgreSQL MCP section, and a shutdown/cleanup section explaining that `docker compose down` preserves the named volume and `docker compose down -v` is the (opt-in only) way to delete it. All commands were verified against `apps/api/project.json`, root `package.json`, and `docker-compose.yml` before being written — no invented scripts. The agent-shell-specific `env -u CLAUDECODE -u OPENCODE` prefix (used internally in prior stories' dev sessions) was deliberately omitted from the README itself, since it is an artifact of this coding agent's shell environment, not a requirement for an external reviewer following the README on a normal machine.
- Task 3: see Debug Log References above — no secret or real `.env` file found anywhere in `harness/bmad`'s history.
- Task 2: an initial clean-checkout dry run was performed against commit `5ce0b36` (host Node `v26.4.0`), but a subsequent code review found that `5ce0b36` never actually contained the rewritten README (the rewrite was still an uncommitted working-tree change), so that run's evidence chain was broken and it is now marked **superseded, not acceptance evidence** in the Debug Log References above. A **corrected re-run** was then performed: the exact current Story 1.7 candidate diff (uncommitted `README.md`, this story file, `sprint-status.yaml`) was transferred into a disposable clone, committed there as a local-only temporary validation commit (`560b60085efc234722390362815fcb15235733d1`), verified to contain the new README, then validated from a genuinely clean second checkout of that commit, under Node 24 (via a disposable `node:24-slim` container, no global host install) rather than the host's Node 26. All validation points passed: `pnpm install --frozen-lockfile`, Postgres healthy, `prisma migrate deploy` only, seed run twice with proven idempotency (15/15), both endpoints verified against actual JSON content, `pnpm exec vitest run` (19/19 passing), `tsc --noEmit` clean, `nx run api:build` clean, and full cleanup (container, disposable Compose project, both temp clones) confirmed removed. See the "CORRECTED clean-checkout validation" block in Debug Log References for the complete transcript — this corrected run, not the superseded one, is the accepted AC1 evidence.
- All 3 acceptance criteria are now satisfied: AC1 (documented clean-checkout dry run, full sequence passing) via Task 2; AC2 (README never instructs committing real `.env`, references `.env.example`) via Task 1; AC3 (no secret/real `.env` in branch history) via Task 3.

### File List

- `README.md` (modified — full rewrite, replacing Nx boilerplate)
- `_bmad-output/implementation-artifacts/1-7-documentation-and-clean-checkout-validation.md` (this file — all tasks marked complete, Dev Agent Record updated, status set to `review`)
- `_bmad-output/implementation-artifacts/sprint-status.yaml` (story 1-7 status updated to `review`)

## Change Log

- 2026-07-18: Story drafted from `epics.md` Story 1.7 and the approved architecture spine. Status set to `ready-for-dev`.
- 2026-07-18: Prisma migration decision made explicit (`migrate deploy` only, never `migrate dev`, no migration file may be created/modified during clean-checkout validation) across Task 1, Task 2, Dev Notes, Exclusions, and Validation Commands, per explicit user instruction, before implementation began.
- 2026-07-18: Implementation started. Task 1 (README rewrite) and Task 3 (git-history secret scan, scoped to `harness/bmad`) completed. Status moved to `in-progress`. Task 2 (clean-checkout dry run) pending a separate approval of its temporary-directory/Docker-isolation plan before execution.
- 2026-07-18: Clean-checkout validation plan approved with mandatory hardening (unique per-run temp dir/Compose project, explicit branch-pinned clone, `trap`-based guaranteed cleanup, `pnpm exec`-only invocation, JSON-content endpoint assertions, readiness loops). Task 2 executed against commit `5ce0b36`; all validation points passed. All 3 acceptance criteria satisfied. Status moved to `review`.
- 2026-07-19: Consolidated code review found the 2026-07-18 Task 2 run's evidence chain broken: commit `5ce0b36` never contained the rewritten README (it was still an uncommitted working-tree change at run time), so the run could not prove the new README's instructions were followed; that run's Node version (host `v26.4.0`) also did not match the repository-declared `>=24 <25` range. That run is now marked superseded in the Debug Log References, not treated as acceptance evidence. Task 2 was re-run per an explicitly approved plan: the exact current candidate diff was transferred into a disposable clone, committed there as a temporary local-only validation commit (`560b60085efc234722390362815fcb15235733d1`), verified to contain the new README, validated from a clean second checkout of that commit under Node 24 (disposable `node:24-slim` container; no global host install), with pnpm 11.9.0 selected explicitly as a validation-harness-only choice (the candidate's `package.json` declares no `packageManager`/`engines.pnpm`). All validation points passed; full cleanup confirmed (container, disposable Compose project/network/volume, both temp clones all removed). Status remains `review`, pending code review sign-off on this corrected evidence — no other blocking findings identified.
- 2026-07-19: Final closure code review (multi-layer: Blind Hunter, Edge Case Hunter, Acceptance Auditor) run against the current uncommitted diff (`README.md`, this story file, `sprint-status.yaml`). Verdict: **PASS**. Independently re-verified: `git show 5ce0b36:README.md` is still unmodified Nx boilerplate (confirms the superseded-run finding), README's documented commands all exist in the repo (`apps/api/project.json`, root `package.json`, `docker-compose.yml`), the `/customers/count` and `/customers/by-distance` route implementations match the README's documented response contracts exactly, `.gitignore`/`.env.example` match the README's `.env` guidance, `git log harness/bmad --diff-filter=A -- .env` and a credential-string scan confirm no secret/real `.env` in this branch's history, and `git status` confirms only these 3 files are modified with no application/test/Prisma/Docker/package/lockfile/MCP/dependency change and no later story/epic artifact created. One concrete inconsistency found and fixed in this review: Task 2's checklist bullets cited the superseded run's port (`5434`) and `trap`-based cleanup instead of the accepted run's actual port (`5440`) and explicit sequential cleanup — corrected in place. Four minor items (README troubleshooting gaps, a `.env.example` `DATABASE_URL` caveat not repeated in the README, the unrecoverable temporary validation commit, and stale `sprint-status.yaml` `verification_gate` prose) were routed to `deferred-work.md` as non-blocking. All 3 acceptance criteria confirmed satisfied; all Definition of Done items now checked except "own focused commit" (by design — this review does not stage/commit/push). Status moved to `done`.
